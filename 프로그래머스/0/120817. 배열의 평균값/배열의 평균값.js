function solution(numbers) {
    var answer = 0;
    let sum = 0;
    let count = 0;
    for (number of numbers) {
        sum += number;
        count++;
    }
    answer = sum / count;
    return answer;
}